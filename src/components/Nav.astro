---
const base = import.meta.env.BASE_URL;
const go = (p) => `${base}${String(p).replace(/^\/+/, "")}`;
---
<nav class="flex flex-wrap items-center gap-3">
  <a class="bf-chip no-underline hover:bg-slate-50" href={go("/")}>Home</a>
  <a class="bf-chip no-underline hover:bg-slate-50" href={go("/stores/")}>Stores</a>
  <a class="bf-chip no-underline hover:bg-slate-50" href={go("/offers/")}>Offers</a>
  <a class="bf-chip no-underline hover:bg-slate-50" href={go("/guides/")}>Guides</a>
  <a class="bf-chip no-underline hover:bg-slate-50" href={go("/contact/")}>Contact</a>
</nav>

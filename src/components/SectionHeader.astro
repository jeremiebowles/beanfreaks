---
const { kicker, title, lead } = Astro.props;
---
<div>
  {kicker && <p class="pill-bf bg-ink/5 text-ink">{kicker}</p>}
  <h2 class="mt-4 text-2xl font-extrabold tracking-tight sm:text-3xl">{title}</h2>
  {lead && <p class="mt-3 text-muted">{lead}</p>}
</div>
